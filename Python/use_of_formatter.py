formatter="{} {} {} {}"
print(formatter.format(1,2,3,4))
print(formatter.format('one','two','three','four'))
print(formatter.format(True,False,True,False))
print(formatter.format(formatter,formatter,formatter,formatter))
print(formatter.format(
    "Twinke Twinkle", 
    "little Star",
 "how are wonder", 
 "what you are"))
# in this code the one variable  is replaced the {} so the four variable is replace the  four{} and the formaatter is an function and the format is a function
# true and false is an keyword so no used around in the single and double quotes if you are put in the quotes then they do not work.